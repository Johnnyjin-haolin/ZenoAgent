<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiagent.infrastructure.mapper.DocumentMapper">
    
    <resultMap id="BaseResultMap" type="com.aiagent.domain.entity.DocumentEntity">
        <id column="id" property="id"/>
        <result column="knowledge_base_id" property="knowledgeBaseId"/>
        <result column="title" property="title"/>
        <result column="type" property="type"/>
        <result column="content" property="content"/>
        <result column="metadata" property="metadata"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>
    
    <insert id="insert">
        INSERT INTO document (id, knowledge_base_id, title, type, content, metadata, status, create_time, update_time)
        VALUES (#{id}, #{knowledgeBaseId}, #{title}, #{type}, #{content}, #{metadata}, #{status}, #{createTime}, #{updateTime})
    </insert>
    
    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM document WHERE id = #{id}
    </select>
    
    <select id="selectByKnowledgeBaseId" resultMap="BaseResultMap">
        SELECT * FROM document WHERE knowledge_base_id = #{knowledgeBaseId} ORDER BY create_time DESC
    </select>
    
    <select id="selectPage" resultMap="BaseResultMap">
        SELECT id, knowledge_base_id, title, type, metadata, status, create_time, update_time
        FROM document
        <where>
            knowledge_base_id = #{knowledgeBaseId}
            <if test="keyword != null and keyword != ''">
                AND title LIKE CONCAT('%', #{keyword}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="type != null and type != ''">
                AND type = #{type}
            </if>
        </where>
        <choose>
            <when test="orderBy != null and orderBy != ''">
                ORDER BY ${orderBy}
                <if test="orderDirection != null and orderDirection != ''">
                    ${orderDirection}
                </if>
            </when>
            <otherwise>
                ORDER BY create_time DESC
            </otherwise>
        </choose>
        LIMIT #{limit} OFFSET #{offset}
    </select>
    
    <select id="countByConditions" resultType="int">
        SELECT COUNT(*) FROM document
        <where>
            knowledge_base_id = #{knowledgeBaseId}
            <if test="keyword != null and keyword != ''">
                AND title LIKE CONCAT('%', #{keyword}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="type != null and type != ''">
                AND type = #{type}
            </if>
        </where>
    </select>
    
    <insert id="insertBatch">
        INSERT INTO document (id, knowledge_base_id, title, type, content, metadata, status, create_time, update_time)
        VALUES
        <foreach collection="documents" item="item" separator=",">
            (#{item.id}, #{item.knowledgeBaseId}, #{item.title}, #{item.type}, #{item.content}, #{item.metadata}, #{item.status}, #{item.createTime}, #{item.updateTime})
        </foreach>
    </insert>
    
    <update id="update">
        UPDATE document
        SET title = #{title},
            type = #{type},
            content = #{content},
            metadata = #{metadata},
            status = #{status},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>
    
    <delete id="deleteById">
        DELETE FROM document WHERE id = #{id}
    </delete>
    
    <delete id="deleteByKnowledgeBaseId">
        DELETE FROM document WHERE knowledge_base_id = #{knowledgeBaseId}
    </delete>
    
    <delete id="deleteByIds">
        DELETE FROM document WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
    
    <select id="existsById" resultType="boolean">
        SELECT COUNT(*) > 0 FROM document WHERE id = #{id}
    </select>
    
</mapper>

