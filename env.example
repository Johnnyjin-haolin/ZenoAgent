# ===========================================
# ZenoAgent 环境变量配置模板
# ===========================================
# 使用方式：复制此文件为 .env，Docker Compose 会自动读取
#   cp env.example .env
# 详细说明见 docs/CONFIG_REFERENCE.md
# ===========================================

# -------------------------------------------
# 1. LLM API Keys（Docker 部署时至少配置一个）
# -------------------------------------------
# Docker 使用 profile/prod/application.yml，通过 apiKey: ${OPENAI_API_KEY:} 注入
# 本地开发使用 profile/local/application.yml，可直接填写 apiKey 或使用环境变量
OPENAI_API_KEY=sk-your-openai-api-key

# 深度求索：若在 profile 中添加 DeepSeek 模型且使用 apiKey: ${DEEPSEEK_API_KEY:} 时需配置
DEEPSEEK_API_KEY=sk-your-deepseek-key

# -------------------------------------------
# 2. MySQL 配置
# -------------------------------------------
MYSQL_PORT=3306
MYSQL_ROOT_PASSWORD=root123456
MYSQL_DATABASE=zeno_agent
MYSQL_USER=zenoagent
MYSQL_PASSWORD=zeno123456

# -------------------------------------------
# 3. PostgreSQL 配置（RAG 功能可选）
# -------------------------------------------
# 仅在启用 RAG 知识检索时需要
# PG_HOST=localhost
# PG_PORT=5432
# PG_DATABASE=zeno_agent
# PG_USER=rag_user
# PG_PASSWORD=Rag@123456

# -------------------------------------------
# 4. 后端配置
# -------------------------------------------
BACKEND_PORT=8080
SPRING_PROFILES_ACTIVE=prod

# -------------------------------------------
# 5. 前端配置
# -------------------------------------------
FRONTEND_PORT=5173
# 前端请求后端 API 的地址，生产环境改为实际地址
# 如：https://api.example.com
VITE_API_BASE_URL=http://localhost:8080

# -------------------------------------------
# 6. AI Agent 模型配置
# -------------------------------------------
AIAGENT_MODEL_DEFAULT_MODEL_ID=gpt-4o-mini
