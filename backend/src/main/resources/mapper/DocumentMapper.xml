<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiagent.mapper.DocumentMapper">
    
    <resultMap id="BaseResultMap" type="com.aiagent.entity.DocumentEntity">
        <id column="id" property="id"/>
        <result column="knowledge_base_id" property="knowledgeBaseId"/>
        <result column="title" property="title"/>
        <result column="type" property="type"/>
        <result column="content" property="content"/>
        <result column="metadata" property="metadata"/>
        <result column="status" property="status"/>
        <result column="failed_reason" property="failedReason"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>
    
    <insert id="insert">
        INSERT INTO document (id, knowledge_base_id, title, type, content, metadata, status, failed_reason, create_time, update_time)
        VALUES (#{id}, #{knowledgeBaseId}, #{title}, #{type}, #{content}, #{metadata}, #{status}, #{failedReason}, #{createTime}, #{updateTime})
    </insert>
    
    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM document WHERE id = #{id}
    </select>
    
    <select id="selectByKnowledgeBaseId" resultMap="BaseResultMap">
        SELECT * FROM document WHERE knowledge_base_id = #{knowledgeBaseId} ORDER BY create_time DESC
    </select>
    
    <insert id="insertBatch">
        INSERT INTO document (id, knowledge_base_id, title, type, content, metadata, status, failed_reason, create_time, update_time)
        VALUES
        <foreach collection="documents" item="item" separator=",">
            (#{item.id}, #{item.knowledgeBaseId}, #{item.title}, #{item.type}, #{item.content}, #{item.metadata}, #{item.status}, #{item.failedReason}, #{item.createTime}, #{item.updateTime})
        </foreach>
    </insert>
    
    <update id="update">
        UPDATE document
        SET title = #{title},
            type = #{type},
            content = #{content},
            metadata = #{metadata},
            status = #{status},
            failed_reason = #{failedReason},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>
    
    <delete id="deleteById">
        DELETE FROM document WHERE id = #{id}
    </delete>
    
    <delete id="deleteByKnowledgeBaseId">
        DELETE FROM document WHERE knowledge_base_id = #{knowledgeBaseId}
    </delete>
    
    <delete id="deleteByIds">
        DELETE FROM document WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
    
    <select id="existsById" resultType="boolean">
        SELECT COUNT(*) > 0 FROM document WHERE id = #{id}
    </select>
    
</mapper>

